def configure(conf):
    pass


def build(bld):
    # Bulid
    bld.program(
        source='main.cpp',
        target="../../Debug/01helloworld",
        cxxflags=['-fpermissive'],
        use=['Debug/kge-0.1'],
    )
