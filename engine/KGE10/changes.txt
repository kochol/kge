=== 0.1.0 ===

* Core
- (Add) Settings class for managing all settings
* Renderer
- (Add) CreateTexture interface added
- (Add) HardwareBuffer interface for working with buffers on GPU
- (Add) CustomVertexElement
- (Add) VertexDec
	* Renderer_d3d9
	- (Add) VertexBufferDX9 class added for working with Vertex buffers.
	- (Add) IndexBufferDX9 class added for working with index buffers.
	- (Add) TextureDX9 class for working with textures in D3D9.
	* Renderer_ogl
	- (Add) VertexBufferOGLvbo class added for working with VBO in OpenGL.
	- (Add) indexBufferOGLvbo class added for working with VBO in OpenGL.
	- (Add) TextureOGL class for working with textures in OpenGL.
	* Renderer_d3d11
* FileSystem
- (Add) FileSystemManager
- (Add) FileSystem
- (Add) File
- (Add) Stream
* Loaders
- (Add) Loader class is an interface for resource loaders in engine
- (Add) MeshLoader class is an interface for mesh loaders in engine
    * Loader_OGRE
	- (Add) OgreMeshLoader class for loading OGRE mesh files
	* Loder_DDS
	- (Add) DDS texture loader plugin
* Math
- (Add) Matrix class with SSE2 support added.
- (Add) Vector class
* EntitySystem
- (Add) Component
- (Add) ComponentType
- (Add) Entity
- (Add) EntityManager
- (Add) World
- (Add) WorldManager
* Tools
- (Add) libRocketKGERenderer